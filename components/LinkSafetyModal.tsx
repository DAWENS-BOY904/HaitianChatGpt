Hello AI,

When I send a text, always read it fully before making any changes. Do not remove anything without first understanding it. If you need to modify something, make the changes carefully and do not skip any part.

For this page, apply a blur mode effect to all content.

Additionally, when the user leaves the home screen while the AI is generating a response, the conversation should continue running in the background (using a background task or AppState listener). Once the AI finishes responding, send a local push notification that includes the first 60 characters of the AI’s reply. This allows the user to tap the notification and return to view the full response.

Make sure nothing is skipped and all requested changes are applied.
