/**
 * Native pass-through for react-native-purchases.
 * On iOS/Android we simply re-export the real package.
 */
export { default, LOG_LEVEL, PURCHASE_TYPE, INTRO_ELIGIBILITY_STATUS } from 'react-native-purchases';
