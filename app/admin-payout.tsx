make this real and clear.In admin-verify.tsx, add a multi-select mode to the bug reports list that lets admins select multiple reports and update all their statuses at once with a single action button.In admin-verify.tsx, after approving a role application, automatically send an email to the applicant using the send-admin-email edge function notifying them of the approval decision.Add a stats summary card at the top of the Applications tab in admin-verify.tsx showing total counts per status (pending, reviewing, approved, rejected) as colored pill badges.
