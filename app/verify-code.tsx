update supabase/functions/send-verification-code/index.ts to also support a 'registration' type that sends a branded signup verification email via Resend, so new user registration codes go through the same Resend pipeline as password changes.Update the handleVerify function in verify-code.tsx to exclusively use the send-verification-code edge function (Resend API) for code verification — remove all Supabase OTP fallback paths so verification codes are only sent and checked via Resend.
