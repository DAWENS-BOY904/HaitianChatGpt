add real analytics allow if on desktop version also add home desktop version better Add a simple bar chart to the Admin Stats tab showing daily revenue for the last 7 days, using data from the subscription_purchases table grouped by date, rendered with React Native Views (no chart library needed).Add a Push Notification tab to admin-email.tsx that lets admins send a push notification to all users with push tokens stored in user_profiles.push_token, via the send-admin-email edge function or a new edge function.
