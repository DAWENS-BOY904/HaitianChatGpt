In login.tsx and the auth flow, after a web login is detected, insert a pending record into activity_logs with action_type='web_login_request' and expires_at=10min, then poll every 3 seconds for the approval_status to become 'approved' before redirecting — show a waiting screen with a cancel option if not approved within 10 minutes Create a new edge function `supabase/functions/notify-web-login/index.ts` that accepts a userId and session details, reads the push_token from user_profiles, and sends an Expo push notification to the user's mobile device with a deep link to /new-device-verify?requestId=xxx so they can approve or deny the web login.
